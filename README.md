# Service samples

Two small, standalone projects live in the directories below (clone path may match the repository name).

- **`code-smells-1`** — Social posts & chat API (FastAPI + Beanie + MongoDB)
- **`code-smells-2`** — Ops console (Nuxt 3 + TypeScript)

Each folder has its own README with how to run it and what it does.
