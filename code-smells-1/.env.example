# Reference values — export into your shell (or use a process manager) before uvicorn.
# Example: export MONGODB_URL=mongodb://localhost:27017 DATABASE_NAME=catalog_api
# Start Mongo: docker compose up -d
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=catalog_api
